# shared/utils

Utility functions ที่ใช้ร่วมกันระหว่าง frontend และ backend

**Round 41A:** Utils ยังอยู่ที่ `src/lib/utils.ts` ตามเดิม
จะค่อย ๆ ย้ายมาที่นี่เมื่อพร้อม โดยมี re-export กลับ

ตัวอย่าง utils ที่จะย้ายมา:
- formatDate, formatDateTime
- cn (className merging)
- slug generation
