/**
 * Backend service stub: site settings
 * Round 41A: stub only — API route src/app/api/admin/settings/route.ts handles logic directly
 * Will be wired up in a future round after behavior is verified stable
 *
 * Planned: extract upsertSettings(), getSettings() from the API route
 */
