/**
 * Backend service stub: CMS pages
 * Round 41A: stub only — API route src/app/api/admin/pages/route.ts handles logic directly
 * Will be wired up in a future round after behavior is verified stable
 *
 * Planned: extract getPage(), savePage() from the API route
 */
