/**
 * Backend service stub: news/announcements
 * Round 41A: stub only — API route src/app/api/admin/news/route.ts handles logic directly
 * Will be wired up in a future round after behavior is verified stable
 *
 * Planned: extract listNews(), createNews(), updateNews(), archiveNews()
 */
