/**
 * Backend service stub: admin users
 * Round 41A: stub only — API routes src/app/api/admin/users/route.ts and [...id]/route.ts handle logic directly
 * Will be wired up in a future round after behavior is verified stable
 *
 * Planned: extract listUsers(), createUser(), updateUser(), deactivateUser()
 */
